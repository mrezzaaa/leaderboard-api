import { RateLimitterInterceptor } from './rate-limitter.interceptor';

describe('RateLimitterInterceptor', () => {
  it('should be defined', () => {
    expect(new RateLimitterInterceptor()).toBeDefined();
  });
});
